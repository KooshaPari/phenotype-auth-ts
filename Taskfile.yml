version: '3'
tasks:
  install:
    desc: Install dependencies
    cmds:
      - bun install
  build:
    desc: Build the project
    cmds:
      - bun run build
  test:
    desc: Run tests
    cmds:
      - bun run test
  lint:
    desc: Run static checks
    cmds:
      - bun run typecheck
  clean:
    desc: Remove build artifacts
    cmds:
      - rm -rf dist coverage .vitepress/dist
